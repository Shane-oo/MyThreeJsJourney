#include <begin_vertex>

// Elevation
//float amplitude = 1.0;
//float frequency = 2.0;
///vec3 newPosition = transformed + vec3(0.0, amplitude * sin(transformed.x * frequency),0.0);

//transformed = newPosition;

transformed = transformed;

